--- /w/00-src/flash_attention_simplified.cu	2026-08-01 13:35:32.755985200 +0000
+++ /w/02-convert/hipify-out/flash_attention_simplified.hip.cpp	2026-08-01 22:32:52.835308500 +0000
@@ -1,3 +1,4 @@
+#include "hip/hip_runtime.h"
 // ============================================================================
 // FlashAttention 단순화 교육용 커널 (Forward만, 개념 전달 목적)
 //
@@ -15,8 +16,8 @@
 //   softmax 는 "online softmax" 트릭으로 누적 보정.
 // ============================================================================
 
-#include <cuda_runtime.h>
-#include <math_constants.h>   // CUDART_INF_F
+#include <hip/hip_runtime.h>
+#include <hip/hip_math_constants.h>   // HIP_INF_F
 
 // ---- 튜닝 파라미터: 바로 이 숫자들이 "하드웨어 종속" 지점 ----------------
 // Br x d, Bc x d 타일 3~4개가 SM 의 공유 메모리(예: A100 = 최대 164KB)에
@@ -64,7 +65,7 @@
     //     l   : 지금까지의 softmax 분모 (exp 합)
     //     acc : 지금까지의 출력 누적값 (아직 분모로 안 나눈 상태)
     // ------------------------------------------------------------------
-    float m = -CUDART_INF_F;
+    float m = -HIP_INF_F;
     float l = 0.0f;
     float acc[HEAD_DIM];
     #pragma unroll
@@ -90,7 +91,7 @@
         // ---- (4b) 이 타일에 대한 점수 s_j = q . k_j 를 계산하며
         //           타일 내 최댓값 m_tile 을 구한다 ----
         float s[Bc];
-        float m_tile = -CUDART_INF_F;
+        float m_tile = -HIP_INF_F;
         for (int j = 0; j < Bc && (tile + j) < N; ++j) {
             float dot = 0.0f;
             #pragma unroll
